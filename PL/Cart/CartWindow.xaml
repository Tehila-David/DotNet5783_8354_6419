<Window x:Class="PL.Cart.CartWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Cart"
        mc:Ignorable="d"
        Title="CartWindow" Height="450" Width="800">
    <Window.Resources>
        <DataTemplate DataType="OrderItem">
            <Border Name="border" BorderBrush="Aqua" BorderThickness="1"
          Padding="5" Margin="5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Name:"/>
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Path=CartItems.}" />
                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Amount:"/>
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Path=Amount}"/>
                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Price:"/>
                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Path=Price}"/>
                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Total Price:"/>
                    <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding Path=TotalPrice}"/>

                </Grid>
            </Border>
        </DataTemplate>
    </Window.Resources>
    <StackPanel>
        <ListBox ItemsSource="{Binding}"
        HorizontalContentAlignment="Stretch" />
    </StackPanel>

</Window>

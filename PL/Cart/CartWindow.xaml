<Window x:Class="PL.Cart.CartWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Cart"
        mc:Ignorable="d"
        Title="CartWindow" Height="450" Width="800">
    <Window.Resources>
        <DataTemplate DataType="local:MyData">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0"
                 Text="Product Name: " />
                <TextBlock Grid.Column="1"
                Text="{Binding Path=ProductName}" />
                <TextBlock Grid.Column="2"
                Text="Product Amount: " />
                <TextBlock Grid.Column="3"
                 Text="{Binding Path=ProductAmount}" />
                <TextBox Grid.Column="4"
                 Text="Product Price: " />
                <TextBlock Grid.Column="5"
                 Text="{Binding Path=ProductPrice}" />
                <TextBox Grid.Column="6"
                 Text="Product Total Price: " />
                <TextBlock Grid.Column="7"
                 Text="{Binding Path=ProductTotalPrice}" />
            </Grid>
        </DataTemplate>
    </Window.Resources>
    <StackPanel>
        <TextBlock Text="Enter new product amount:" />
        <TextBox/>
        <Button Content="Update product amount" Click="Button_Click"
          />
        <Button Content="Remove Product" Click="Button_Click_1"
          />
    </StackPanel>
</Window>

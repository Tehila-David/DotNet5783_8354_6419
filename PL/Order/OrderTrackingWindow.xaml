<Window x:Class="PL.Order.OrderTrackingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PL.Order"
        mc:Ignorable="d"
        Title="OrderTrackingWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Label Content="Enter an order ID :" 
         FontSize="30" Margin="232,58,310,323" Grid.RowSpan="2"/>
        <Grid x:Name="OrderDetailsGrid" Grid.Row="1" Margin="4,2,-4,-2" Grid.RowSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Label Content="ID:" Grid.Column="1" Margin="237,136,524,26" Grid.Row="0" />
            <TextBox x:Name="idTextBox"
            Text="{Binding ID}" 
            Grid.Column="1" Margin="299,133,339,53" Grid.Row="0" />

        </Grid>
        <Button x:Name="orderTrackingButton" Content="Order Tracking" 
         FontSize="30" Grid.Row="1" Margin="248,186,328,192" Click="OrderTracking_Click"/>

    </Grid>
</Window>
